export class series{
    numero: number;
    nombre: string;
    canal: string;
    temporadas: number;

    constructor(numero: number, nombre: string, canal:string,temporadas:number) {
        this.numero = numero
        this.nombre = nombre
        this.canal = canal
        this.temporadas = temporadas

      }
}