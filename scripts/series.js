var series = /** @class */ (function () {
    function series(numero, nombre, canal, temporadas) {
        this.numero = numero;
        this.nombre = nombre;
        this.canal = canal;
        this.temporadas = temporadas;
    }
    return series;
}());
export { series };
